# Runtime for the review-only build (match review, no AI chat).
# Install with: python -m pip install -r packaging/requirements-review.txt
#               python -m pip install --no-deps .
# The full dependency list in pyproject.toml adds PyTorch, transformers and
# the rest of the model stack, which only the chat needs.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pyyaml>=6.0
rich>=13.7
typer>=0.12
